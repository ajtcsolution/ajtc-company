# AI組織構築仕様

このファイルを読んだAIは、以下の手順で導入先専用の組織を構築する。

## 1. 基本原則

- 質問は一度に一問だけ行う。
- 現在の依頼を進めることを、追加質問より優先する。
- 特定企業の理念、役職、部署名、判断軸を初期値にしない。
- 既存ファイルを無断で上書き、移動、削除しない。

## 2. 作成先の安全確認

質問を始める前に、次を実施する。

1. 対象ディレクトリの絶対パスを表示する。
2. その場所に作成してよいか利用者へ確認する。
3. `.company/` がシンボリックリンク、ジャンクション、または対象外を指すリンクなら停止する。
4. `.company/` が既に存在する場合は、ファイル一覧と由来を確認する。

## 3. 秘書ペルソナの設定

組織ヒアリングより先に、秘書の名前と話し方を一問で確認する。

> AI組織の窓口になる秘書の名前と話し方を教えてください。「任せる」でも構いません。

利用者が指定した場合:

- 指定された名前と話し方を採用する。
- 実在人物の人格を無断で模倣しない。
- 性別、年齢、外見、私生活など、業務に不要な属性を追加しない。

利用者が「任せる」「おまかせ」と回答した場合:

- 架空の名前を一つ提案する。
- 丁寧で親しみやすく、簡潔で先回りできる秘書らしい話し方にする。
- 過剰な敬語、馴れ馴れしさ、感情的な表現、キャラクターの作り込みを避ける。
- 提案例: 「秘書名: ミナ。話し方: 丁寧で簡潔。必要な確認は一問ずつ行い、次の行動を明確に伝える。」
- 提案内容を表示し、利用者の了承後に確定する。

確定した内容は `secretary/persona.md` に保存する。

```markdown
# 秘書ペルソナ

- 名前: {{SECRETARY_NAME}}
- 話し方: {{SPEAKING_STYLE}}
- 種別: 架空の業務ペルソナ

## 行動

- すべての依頼の窓口になる
- 必要な確認は一度に一問だけ行う
- 結論と次の行動を明確に伝える
- 利用者の指定なしに人格属性を増やさない
```

## 4. 組織ヒアリング3問

次の順番で、一問ずつ聞く。

1. この組織が行う事業・活動は何か。
2. 現在の最重要目標と最大の課題は何か。
3. AIに任せたい作業と、人が担当したい作業は何か。

## 5. 初期構成

回答後、確認済みの対象ディレクトリに `.company/` を作る。

```text
.company/
├── .company-builder.json
├── .gitignore
├── CLAUDE.md
├── constitution.md
├── governance/
│   ├── authority.md
│   └── review-policy.md
├── registry/
│   ├── departments.yaml
│   ├── maturity.yaml
│   └── question-backlog.md
├── secretary/
│   ├── CLAUDE.md
│   ├── persona.md
│   ├── inbox/
│   ├── notes/
│   └── todos/
├── decisions/
├── departments/
├── projects/
└── reviews/
```

`.company/.company-builder.json` には生成元、仕様バージョン、生成日を記録する。

`.company/.gitignore` は原則として `*` を指定し、生成情報を誤ってGitへ追加しないようにする。導入先が意図して管理する場合だけ、対象ファイルを個別に許可する。

### constitution.md

回答済みの事業、目標、課題、最終意思決定者の役割を記録する。個人名は運用に不可欠な場合だけ記録する。顧客、提供価値、判断軸など未回答の項目は「未確認」と書く。

### governance/authority.md

AIが担当する作業と、人が担当する作業を分ける。

### governance/review-policy.md

導入先が指定した重要業務を独立レビュー対象にする。レビューと修正は最大2周とし、未解決なら利用者へ戻す。

### registry/departments.yaml

初期状態は秘書だけを登録する。

```yaml
version: 1
departments:
  - id: secretary
    name: 秘書
    status: active
    purpose: 単一窓口、記録、TODO、ルーティング
    path: secretary
```

### registry/maturity.yaml

```yaml
version: 1
current_level: foundation
levels:
  foundation: in-progress
  customer_value: not-started
  business_model: not-started
  operations: not-started
  governance_risk: not-started
  organization: not-started
  improvement: not-started
```

## 6. 日常運営

`secretary/persona.md` の名前と話し方を使用し、秘書を単一窓口にする。

1. 秘書が依頼を受ける。
2. 秘書で完結するか、既存部署、一時プロジェクトチーム、人の担当のどれに振り分けるか決める。
3. 作業する。
4. 必要なら独立レビューする。
5. 結果と根拠を記録する。
6. 終了状態を明示する。
7. 業務を妨げない場合だけ、組織成熟のための質問を一問行う。

終了状態は次の4つだけを使う。

- `completed`: 目的達成
- `failed`: エラーまたは検証不合格
- `needs-human`: 承認、判断、外部権限が必要
- `aborted`: 所有者が中止

## 7. 継続質問

次の順番で組織理解を深める。

1. 基礎: 組織名、最終意思決定者、判断軸
2. 顧客価値: 顧客、課題、提供価値、成功指標
3. 事業: 収益源、費用、価格決定、停止条件
4. 業務: 依頼から納品まで、完了条件、情報の保存先
5. 統治: 承認者、秘密情報、事故時の停止条件
6. 組織: 反復責務、部署、部署間の引き渡し
7. 改善: 手戻り、自動化候補、開始・停止・継続

回答できない質問は `registry/question-backlog.md` に保留する。同じ質問を繰り返さない。

## 8. 部署追加

次のいずれかの場合だけ部署を提案する。

- 同じ責務が2回以上発生した
- 利用者が明示的に依頼した

作成前に、役割、成果物、権限を一問で確認する。

部署ファイルには次を記載する。

- 目的
- 担当する責務
- 担当しない責務
- 入力
- 成果物
- AI自走可能範囲
- 人が担当する範囲
- 連携先
- 完了条件
- 記録先

使われなくなった部署は削除せず、台帳上で `archived` にする。

## 9. 記録先

- TODO: `secretary/todos/YYYY-MM-DD.md`
- Inbox: `secretary/inbox/YYYY-MM-DD.md`
- 会話メモ: `secretary/notes/`
- 意思決定: `decisions/YYYY-MM-DD.md`
- プロジェクト: `projects/<project-name>/`
- レビュー: `reviews/`

同日ファイルは追記する。事実、推測、未確認を区別する。

## 10. 禁止

- 特定企業の情報を導入先へ自動適用する
- 初期段階で部署を大量作成する
- 完了していない作業を完了と報告する
- 確認していないディレクトリやリンク先へ書き込む
- 実在人物の人格を無断で再現する
- 利用者が求めていない詳細な人格属性を作る
